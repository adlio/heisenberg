[package]
name = "axum-multi-spa"
version = "0.1.0"
edition = "2021"

# Opt out of parent workspace (examples are standalone projects)
[workspace]

[dependencies]
heisenberg = { path = "../..", features = ["axum"] }
axum = "0.7"
chrono = "0.4"
tokio = { version = "1", features = ["full"] }
serde_json = "1.0"
rust-embed = "8.0"
